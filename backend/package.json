{
  "name": "semprec",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "check:boundaries": "depcruise --config .dependency-cruiser.cjs --output-type err modules services"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "dependency-cruiser": "^18.2.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.11"
  }
}
