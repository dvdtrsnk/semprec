/** Canonical `databases.owner_module_id` value for the MCP servers system database (issue #123) — the `canonical-keys` skill's established vocabulary. */
export const MCP_SERVERS_MODULE_ID = "mcpServers";
