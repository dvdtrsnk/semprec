/** Canonical `databases.owner_module_id` values for the library module (issue #25) — see the `canonical-keys` skill's established vocabulary. */
export const BOOKS_MODULE_ID = "books";
export const MOVIES_MODULE_ID = "movies";
